<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/styles/reset.css" />
    <link rel="stylesheet" href="/styles/custom.css" />
  </head>
  <body>
    <header>
      <h1>
        <a href="/">sYamaz.github.io</a>
      </h1>
      <div class="x-avater">
      </div>
    </header>
    <main>
      {% if tags and tags.includes("post") %}
        <article>
          {{ content | safe }}
        </article>
      {% else %}
        {{ content | safe }}
      {% endif %}
    </main>
    <footer>
      <small>© {{ year }} Shun Yamazaki</small>
    </footer>
  </body>
</html>