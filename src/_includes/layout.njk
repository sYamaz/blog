{% set ogDescription =
  description
  or (content | excerpt(140))
%}
{% set isPost = tags and ("post" in tags) %}
{% set ogType = "website" %}
{% if isPost %}
  {% set ogType = "article" %}
{% endif %}
<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <!-- OGP -->
    <meta property="og:type" content="{{ ogType }}" />  
    <meta property="og:title" content="{{ title }}" />
    <meta property="og:description" content="{{ ogDescription }}" />
    <meta property="og:url" content="{{ page.url | url }}" />
    <meta property="og:image" content="https://blog.shunyamazaki.com/avater-ogp.jpg" />

    <!-- X -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content="{{ title }}" />
    <meta name="twitter:description" content="{{ ogDescription }}" />
    <meta name="twitter:image" content="https://blog.shunyamazaki.com/avater-ogp.jpg" />

    <!-- Fonts: Noto Serif JP を実配信（本文400・見出し700） -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;700&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/styles/reset.css" />
    <link rel="stylesheet" href="/styles/tokens.css" />
    <link rel="stylesheet" href="/styles/custom.css" />
    <link rel="icon" href="/favicon.ico">
  </head>
  <body>
    <header>
      <h1>
        <a href="/">コードと余白</a>
      </h1>
    </header>
    <main>
      {% block content %}{% endblock %}
    </main>
  </body>
</html>